# ── Runtime user (must match host data directory owner) ──────────────────
# Run: APP_UID=$(id -u) APP_GID=$(id -g) to get these values
APP_UID=1000
APP_GID=1000

# ── Database ──────────────────────────────────────────────────────────────
# Path inside the container. Do not change unless you know what you're doing.
DB_PATH=/app/data/cashflow.db

# ── Security ──────────────────────────────────────────────────────────────
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# REQUIRED — paste a generated value; blank template values make production startup fail.
SECRET_KEY=

# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
# REQUIRED — paste a generated value; blank template values make production startup fail.
SESSION_ENCRYPTION_KEY=

# ── CORS ──────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins. Set to your production domain.
ALLOWED_ORIGINS=https://your-domain.com

# ── Timezone ──────────────────────────────────────────────────────────────
TZ=Europe/Rome

# ── Cookie ────────────────────────────────────────────────────────────────
# Defaults to true (Secure flag set). Set to false only when serving over
# plain HTTP on a non-localhost hostname (e.g. Tailscale or LAN access).
# COOKIE_SECURE=false
